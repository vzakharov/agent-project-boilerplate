{
  "repo": "Playgramai/playgramapp",
  "lastSyncedSha": "4cd68a75aacff81442e6d0dc9d5219f3b6015de6",
  "lastSyncedAt": "2026-08-11",
  "adopted": [
    "CLAUDE.md",
    "README.md",
    ".claude/",
    "scripts/",
    {
      "scripts/run-parallel.py": "taken as a POSIX sh port, scripts/run-parallel.sh, generalized from pnpm script names to <label>=<command> pairs — vet.sh is not escapable, so it must not require python3. Read later upstream changes for the orchestration idea and port by hand; the file itself never lands here."
    },
    {
      "scripts/export-github-item.ts": "taken as a Python port, scripts/export-github-item.py — the algorithm tracks upstream, the HTTP client does not. undici strips Authorization across a cross-host redirect and ignores HTTPS_PROXY; stdlib urllib does neither, so _DropAuthOnHostChange and the proxy-bypass opener here answer failure modes upstream cannot have. Port behavior changes by hand — a file-level diff compares two languages and says nothing."
    }
  ],
  "declined": {}
}
