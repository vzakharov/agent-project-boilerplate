Proposed squash title/body:

```
docs: #34 stop asking squash bodies for editing traps (pr #35)
```

```
The tighten pass licensed the thing it exists to cut. Step 3's target
sentence asked every body for "anything that would trip someone editing
that area later", so a squash message could grow a maintenance manual
and pass its own review — and the measured caps don't close it, since
such a paragraph fits inside 50 lines at 72 wide as easily as anything
else.

That clause is gone, and the editing trap joins the list of what fails
Lens A. A constraint that has to be obeyed goes where the person about
to break it is already looking — a `.claude/rules/` file, or a
docstring on the thing itself — unless the change is that the
constraint now exists. Cutting one is a move rather than a deletion, so
the home for each item is named and checked as it goes: the body is
sometimes the only place a constraint was ever written down, and the
tidy cut is what loses it.

Step 2 hands the run a live working file and said nothing about what to
do with it, so the cheap refresh appends the new scope and leaves the
rest standing. That walks the body past the cap one push at a time, and
leaves superseded wording beside what replaced it — the first caught
late by the caps, the second by nothing. Step 3 now states that a re-run
cuts and replaces, re-applying the paragraph target and the caps to the
whole body every run.

Closes #34

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
