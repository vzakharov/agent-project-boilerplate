Proposed squash title/body:

```
feat: rename /sync-muthur to /update-muthur (pr #67)
```

```
The skill that pulls vendored agent infrastructure forward is named for
what it does to the tree it runs in, so the command is `/update-muthur`
— `npm update`, not a two-way sync. The directory moves with it, and so
do the catalog row, the `CATALOG`/`WATERMARK` constants in the two
checking scripts, and every citation in the prose an adopter reads.

The watermark's fields keep the old verb. `lastSyncedSha` and
`lastSyncedAt` are on disk in every downstream adopter's
`watermark.json`, so renaming them to match the command would orphan all
of those files at once. The skill now states that split, so the next
reader does not finish the rename by breaking the format, and the
invocation phrases answer to both verbs — the operation is still a sync
whatever the command is called.

`/spinoff` finds a caller's watermark by what the file contains rather
than where it sits, because neither path segment is stable downstream
and no rename is pushed there. Its list of the paths it may encounter
gains the one this change retires.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
