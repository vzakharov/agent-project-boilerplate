Proposed squash title/body:

```
fix: never resolve GitHub comment threads (pr #26)
```

```
The operator's review flow depends on thread resolution being theirs
alone: they read down the agent's replies and resolve the threads that
satisfy them, leaving the rest open as the list of what still needs
attention. An agent that resolves the threads it addressed — which the
harness's PR-driving rules explicitly instruct — deletes items from
that list before the operator has read them, and a comment that was
never attended to becomes indistinguishable from one that was.

CLAUDE.md's "GitHub comments" section now forbids it outright: reply,
and leave the thread open, however settled the point looks — a pushed
fix, a verified non-issue, a declined ask with reasons. The reverse
move is barred too, since the whole resolution state belongs to the
human: no un-resolving, no re-opening.

The rule states that it overrides any contrary harness or skill
instruction. That clause is load-bearing rather than boilerplate: the
conflicting instruction is one the agent sees in every session that
drives a PR, so a project rule that merely stated the preference would
lose to it.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
