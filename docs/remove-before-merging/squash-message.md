Proposed squash title/body:

```
docs: #33 state that merge state is not an attach-and-work lane (pr #36)
```

```
The remote/web harness's system prompt orders a merge conflict or red
CI fixed "at every event and every check-in", ahead of review
comments. This loop stages both inside /finalize — the base merge at
its Step 2, vetting at its Step 1 — but no skill said so, and an
attach-and-work run with nothing local to weigh against the harness
merged and pushed a conflict resolution before reading what the branch
it was handed actually needed. An unstated rule loses to a stated one.

CLAUDE.md's "Key principles" now states the general form: where the
host harness's standing instructions and this loop disagree about when
work happens, the loop's staging wins and the harness's urgency is
reported rather than acted on. That is the single home for the rule,
and it reaches CI as much as merge state — the same harness block
orders failures fixed per event, against this repo's vet-at-milestones
rule.

/from-branch, /handle and /go carry pointers to it, each stating only
what it alone adds: what the mergeable/mergeStateStatus fields the
attach step fetches are for (the report, and /handle Step 2's input),
that a conflicted or red PR is reported while the discovered lane runs
anyway, and that the base merge belongs to /finalize. Either becomes a
session's work only on `and finalize` or a direct ask, so the rule
gates the unasked case alone. Issue #33 placed the last of these in
/implement, which is now a bare redirect to /go.

Closes #33

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
