Proposed squash title/body:

```
fix: #38 title a sync's squash record by what changed (pr #39)
```

```
A sync PR's squash record kept naming the transport rather than the
cargo. The most concrete-looking fact in front of the agent composing
one is the source SHA it just synced to, so the title's `<essence>`
resolved to "sync forward to <sha>" — a commit in another repository,
unresolvable from the log the title lands in, sending every future
reader to the diff. The body inverted the same mistake, spending
several of its scarce lines on provenance that upstream.json's
lastSyncedSha already records precisely one file over.

`/sync-upstream` Step 8 now carries both halves of the rule: the title
names what landed in this tree, and the upstream connection — the
honest why — earns one clause of the body's opening sentence and
nothing more. Step 8 is where the sync hands off to `/pr` and already
says what belongs in the PR body, so the sync-specific rule sits beside
it rather than in `/squash-message`, whose generic "the transport is
not the essence" form would be lifted on a single observed instance
into the longest skill in the loop. ADOPTING.md's sync section gets a
pointer, since adopters meet this failure on every sync and a `chore:`
subject is the first thing their log shows.

Fixes #38

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
