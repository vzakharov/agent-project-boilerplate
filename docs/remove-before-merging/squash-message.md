Proposed squash title/body:

```
feat: #33 stage harness urgency behind the loop's own steps (pr #36)
```

```
The remote/web harness's system prompt orders a merge conflict or red
CI fixed "at every event and every check-in", ahead of review
comments. This loop stages both inside /finalize — the base merge at
its Step 2, vetting at its Step 1 — but no skill said so, and an
attach-and-work run with nothing local to weigh against the harness
merged and pushed a conflict resolution before reading what the branch
it was handed actually needed. An unstated rule loses to a stated one.

CLAUDE.md's "Key principles" now states the general form: where the
host harness's standing instructions and this loop disagree about when
work happens, the loop's staging wins and the harness's urgency is
reported rather than acted on. It reaches CI as much as merge state,
since the same block orders failures fixed per event against this
repo's vet-at-milestones rule. /from-branch, /handle and /go point at
it, each adding only what it alone knows: what the attach step's
mergeable/mergeStateStatus fields are for, that a conflicted or red PR
is reported while the discovered lane runs anyway, and that the base
merge belongs to /finalize. Either becomes a session's work only on
`and finalize` or a direct ask, so the rule gates the unasked case
alone.

Riding along, a change to the loop is now feat: or fix: rather than
docs:, since the loop is what this repo ships and the Markdown shape of
a diff says nothing about that. The scope is what a session executes —
skills, rules, this file's conventions, the scripts they call —
leaving docs: for prose about the repo and for the working artifacts
/finalize sweeps, so /issue's and /squash-message's own commit examples
stay correct as written. Adopters invert the rule, and it says to
delete it on adoption.

Closes #33

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
