Proposed squash title/body:

```
fix: stop /issue owning implementation, rank what the sweep keeps (pr #23)
```

```
/issue is rescoped to the issue side. It had restated the whole
lifecycle — export, split, implement, open a draft PR — so a session
could conclude, correctly per the text, that it owned
implementation and needed no plan. What kept that path unreachable
was CLAUDE.md's "assume you were launched in plan mode" rule, which
a skill adopted into another repo travels without: the defect is
worse here than at its source, because being adopted from is what
this repo is for. /issue now exports, reads and splits, then hands
/pr a task plus the issue number to close. A native plan-mode launch
is refused outright, the flow needing a committed export and plan
file that plan mode cannot write, and a split stops for approval
before filing a natively-linked sub-issue per slice — the first
included, so the PR has a child to close. /pr gains an <issue>
parameter that beats its inference from commit bodies, and
/qa-checklist stops claiming /issue as a co-owner.

/audit-github-backlog prioritises what it keeps: KEEP and REFILE
require a P0-P3 tier and the closing verdicts forbid one, so an
analyst working from a stale copy of the rules fails the merge
rather than landing unprioritised verdicts. Two GitHub findings
travel with it — a pull request accepts no close reason, so its
modality has to live in the closing comment, and duplicate_of set
state_reason but rendered no link at the source repo, ported as a
caveat rather than as a fact this repo can assert. CLAUDE.md gains
the criterion that separates a decision doc from the code: a line
belongs there if it answers why we chose this, and in the code if it
answers how it works.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
