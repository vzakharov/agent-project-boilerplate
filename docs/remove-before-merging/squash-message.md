Proposed squash title/body:

```
feat: #44 run the skill-catalog check inside the vet run (pr #45)
```

```
`scripts/vet.sh` called `check-squash-message.sh` and nothing else, so
`check-skill-catalog.sh` — the check that catches a dangling
`@.claude/skills/<name>/SKILL.md` pointer, a skill missing its catalog
row, or a stub marker disagreeing with its hydration state — ran only
when an agent remembered a line in CLAUDE.md. Its failure mode is
silent: the agent follows the surviving prose past the step it could
not load, so nothing surfaces at the point of breakage and the loop
loses that step for every session afterwards. A convention that has to
be remembered protects against forgetting exactly as well as
remembering does.

The vet run now calls it beside the squash-message check, so a broken
cross-reference stops a push of review-ready work. Both lines are
non-stack-specific, and the ADOPTERS comment at the top of `vet.sh` is
written around the pair — an adopter replacing everything around them
is told, per line, what dropping it costs.

CLAUDE.md § "Adding or renaming a skill" now states that vetting
covers the check rather than instructing a separate run, leaving the
convention one home; ADOPTING.md and `docs/catalog.md` follow the same
one-line-to-two change, and the catalog's `vet.sh` row lists both
scripts it pulls in.

Closes #44

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
