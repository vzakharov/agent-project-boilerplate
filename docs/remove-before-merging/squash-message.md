Proposed squash title/body:

```
feat: #8 adopt /bootstrap-workflow-dispatch as a G5 skill (pr #13)
```

```
A brand-new `workflow_dispatch` workflow is not dispatchable until
Actions metadata has seen it once, so a feature branch gets
`workflow <file>.yml not found on the default branch` until a one-shot
branch-scoped push trigger registers it. /bootstrap-workflow-dispatch
is the four-step routine that registers the workflow and cleans up
after itself; the quirk bites exactly when an adopter of this
boilerplate first adds a dispatch-only workflow. It lands working
rather than as a G6 stub because it names no stack, language, test
runner or build system.

Its documentation follows the one-description-per-item convention: a
single G5 catalog row, group- and list-level mentions in README and
CLAUDE.md, and a Related section bounding it against the skills it sits
between — /test-on-gh is the dispatch that hits the 404 first,
/watch-ci watches the run a successful dispatch produces. Adding it
also corrects G5's "G4 is required on the web" claim, which was
group-wide but true only of /watch-ci, whose `gh run watch`
long-polling the agent proxy blocks; this skill dispatches over REST
and works unshimmed.

Closes #8

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
