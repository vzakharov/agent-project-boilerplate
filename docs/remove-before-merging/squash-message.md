Proposed squash title/body:

```
fix: write commit SHAs bare in GitHub comments (pr #61)
```

```
A commit SHA written inside a code span renders as inert text on
GitHub. Review replies from the loop cite the commit they pushed, so
the backticks cost each of those replies the one thing it was posted
for: a hash the reviewer can click through to the change.

CLAUDE.md's GitHub-comments section is the home for the rule, and
scopes it to every SHA in a comment rather than only a reply's.
/finalize's two attestation templates held the only backticked SHA
placeholders in the tree; theirs now render bare, with a bullet
citing the rule instead of restating it.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
