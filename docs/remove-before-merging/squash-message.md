Proposed squash title/body:

```
feat: #8 adopt /bootstrap-workflow-dispatch as a G5 skill (pr #13)
```

```
Issue #8 asked to adopt this skill from the donor repo on the strength
of it being a general "dispatch any workflow and report back" routine.
It is not that, and no such skill exists in the donor at all — the name
was conflated in recollection. Adopting it is still right for a
narrower reason: it clears one GitHub quirk that bites exactly when an
adopter of this boilerplate first adds a dispatch-only workflow, and
every acceptance criterion on the issue is satisfiable as written.

The quirk is that a brand-new `workflow_dispatch` workflow is not
dispatchable until Actions metadata has seen it once, so a feature
branch gets `workflow <file>.yml not found on the default branch` until
a one-shot branch-scoped push trigger registers it. The skill lands
working rather than as a G6 stub because the donor file names no stack,
language, test runner or build system — it is generic over
`gh workflow run`, `git push`, and Actions metadata. Two donor-specific
details needed translating: a Notes line citing the donor's own PR and
workflow file, dropped as unreadable from any adopter's repo, and
heading punctuation.

Documentation follows the one-description-per-item convention — the
catalog carries the description in a single G5 row, README and
CLAUDE.md name the skill at group and list level, and the skill's own
Related section marks where it stops: it ends the moment
`gh workflow run` stops 404ing, with /test-on-gh the dispatch that hits
that 404 first and /watch-ci the watcher of the run a successful one
produces. Two corrections fall out. G5's "G4 is required on the web"
claim was group-wide but true only of /watch-ci, whose `gh run watch`
long-polling the agent proxy blocks; this skill dispatches over REST
and works unshimmed. And upstream.json stays untouched deliberately:
`adopted` already covers `.claude/` at directory granularity, and
`lastSyncedSha` asserts a triage that adopting one file out of band
does not make truer.

Closes #8

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
